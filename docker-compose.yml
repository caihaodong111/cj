# 修复版 docker-compose.yml
# 适用于服务器上直接包含Dockerfile文件的情况
# 移除了过时的version属性
# 使用正确的Dockerfile路径

services:
  backend:
    image: mediacrawler-backend:v0.1
    container_name: mediacrawler-backend
    ports:
      - "8000:8000"
    restart: unless-stopped
    networks:
      - mediacrawler-network

  frontend:
    image: mediacrawler-frontend:v0.1
    container_name: mediacrawler-frontend
    ports:
      - "80:80"
    restart: unless-stopped
    depends_on:
      - backend
    networks:
      - mediacrawler-network

networks:
  mediacrawler-network:
    driver: bridge
